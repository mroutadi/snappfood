declare global {
  interface Vendor {
    link: string,
    cover: string,
    logo: string,
    title: string,
    description: string,
    isExpressDelivery: boolean,
    deliveryPrice: number
  }
}